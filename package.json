{
  "name": "forbes-marshall-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "db",
    "web"
  ],
  "scripts": {
    "db:migrate": "node db/migrate.js",
    "db:seed": "node db/seed.js",
    "dev:server": "npm run dev --workspace=server"
  },
  "dependencies": {
    "jspdf": "^4.2.1"
  }
}
